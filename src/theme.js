import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    background: {
      default: '#fbfbfb'
    }
  },
  typography: {
    h2: {
      fontSize: '2.50rem'
    },
    h3: {
      fontSize: '2.25rem'
    },
    h4: {
      fontSize: '1.75rem'
    },
    h5: {
      fontSize: '1.25rem'
    },
    h6: {
      fontSize: '1rem'
    }
  }
})
